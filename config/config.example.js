class Config {

    constructor(){
            this.port = 0000;
            this.database = {
            host: 'host',
            user: 'user',
            password: 'password',
            database: 'DB name'
        };
    }
}

module.exports = Config;